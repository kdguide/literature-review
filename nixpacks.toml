# 文献综述助手 - Railway Nixpacks 配置
# 从源码构建，不依赖 Dockerfile

[phases.build]
cmds = ["npm install", "npm run build", "npm run db:generate"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npm run db:migrate && npm start"
