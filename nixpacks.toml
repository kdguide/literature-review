# Railway Nixpacks 配置
# dist/ 已预构建，跳过构建步骤

[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc"]
# 默认会运行 npm install（安装 better-sqlite3 等原生模块）

[phases.build]
# 空数组 = 跳过构建，不运行 npm run build
cmds = []
