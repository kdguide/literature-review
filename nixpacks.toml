# 文献综述助手 - Railway Nixpacks 配置
# 迁移文件已预生成在 db/migrations/，无需在构建时生成

[phases.build]
cmds = ["npm install", "npm run build"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npm run db:migrate && npm start"
